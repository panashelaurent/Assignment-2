# Assignment-2
Backend fixtures service  for Fantasy Premier League
install all the packages using npm install

create a .env file and paste the info below 

NODE_ENV=development
PORT=5002

# Database
DB_USERNAME=postgres
DB_PASSWORD=Post password
DB_DATABASE=Database name 
DB_HOST=localhost
DB_PORT=5432
DB_DIALECT=postgres

# JWT
JWT_ACCESS_SECRET=66T24hygtaal_71q
JWT_ACCESS_EXPIRES_IN=24h


# File Upload
UPLOAD_DIR=src/uploads


# Email Verification
FRONTEND_URL=http://localhost:3000


run nodemon in console 